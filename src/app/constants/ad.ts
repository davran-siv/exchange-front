export enum AdStatus {
  approved = 'approved',
  banned = 'banned',
  underConsideration = 'underConsideration',
  deleted = 'deleted',
  closed = 'closed',
  notFullFilled = 'notFullFilled'
}

export enum AdType {
  service = 'service',
  product = 'product'
}
